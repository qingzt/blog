{
  "name": "astro-github-blog",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build && pagefind --site dist",
    "build:vercel": "astro build && pagefind --site .vercel/output/static",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "@unocss/reset": "^66.5.12",
    "pagefind": "^1.3.0",
    "typescript": "^5.7.0",
    "unocss": "^66.5.12"
  }
}
